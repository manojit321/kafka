Run container in cli mode

    docker-compose -f docker-compose.yml up

Run container in background mode

    docker-compose -f docker-compose.yml up -d

Commands
    
    docker ps
    docker-compose down
    docker exec -it kafka /bin/sh 
